<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
<a href="/render/MakePosts">POST A PAGE</a>
    <% posts.forEach(posts => {%>
    <div style="border:1px solid black; padding: 20px;" >
    <div> <%= posts.title %></div>
    <div> <%= posts.tag %></div>

    <a href="/render/posts/<%=posts.id%>">edit...</a>
    </form>
    
    <style>
    .btns{
        display: none;
    }
    </style>
    <button id = 'btn'script>delete</button>
    <div class="btns">
    <form action="posts/<%= posts.id %>?_method=DELETE" method="POST">
    <button id="delete-btn" onclick=confirmDelete>delete</button>
    </form>
    <button id='cancel-btn'>cancel</button>
    </div>
    </div>
    <% })%>
</body>
    <script>
    const A = document.getElementById('btn');
    const B = document.getElementById('cancel-btn');
    A.addEventListener('click',()=>{
        document.querySelector('.btns').style.display = 'flex';
    })
    B.addEventListener('click',()=>{
        document.querySelector('.btns').style.display = 'none';
    })
    </script>
</html>